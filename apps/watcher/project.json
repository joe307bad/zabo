{
  "root": "apps/watcher",
  "sourceRoot": "apps/watcher/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/node:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/watcher",
        "main": "apps/watcher/src/main.ts",
        "tsConfig": "apps/watcher/tsconfig.app.json",
        "assets": ["apps/watcher/src/assets"]
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false,
          "fileReplacements": [
            {
              "replace": "apps/watcher/src/environments/environment.ts",
              "with": "apps/watcher/src/environments/environment.prod.ts"
            }
          ]
        }
      }
    },
    "serve": {
      "executor": "@nrwl/node:execute",
      "options": {
        "buildTarget": "watcher:build"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/watcher/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/watcher"],
      "options": {
        "jestConfig": "apps/watcher/jest.config.js",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
